{
    "name": "server",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "node ./bin/www",
        "dev": "nodemon ./bin/www",
        "build-start": "cd ../client && npm run build && cd ../server && npm run start-server",
        "build-dev": "cd ../client && npm run build && cd ../server && npm run dev-server"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "ejs": "^3.1.8",
        "express": "^4.18.1"
    },
    "devDependencies": {
        "nodemon": "^2.0.16"
    }
}
